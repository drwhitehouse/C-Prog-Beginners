#include <stdio.h>
int main() {
    int firstNumber = 25;
    printf("First Number: %d\n", firstNumber);
    // "%d" is a "format specifier"
    int secondNumber = firstNumber;
    printf("Second Number: %d\n", secondNumber);
    return 0;
}
