#include <stdio.h>
int main() {
    int firstNumber, secondNumber = 25;
    // we dont assign anything to firstNumber and it gets some random value.
    printf("First Number: %d\n", firstNumber);
    printf("Second Number: %d\n", secondNumber);
    return 0;
}
